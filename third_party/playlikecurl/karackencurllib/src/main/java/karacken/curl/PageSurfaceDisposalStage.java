package karacken.curl;

public enum PageSurfaceDisposalStage {
    NONE,
    SETTLEMENT_CANCEL_CALLBACK,
    PRE_GL_SETUP,
    SURFACE_RESUME,
    GL_QUEUE_UNAVAILABLE,
    GL_RENDERER_DISPOSE,
    MAIN_TERMINAL_EXECUTOR,
    DECK_RELEASE_CALLBACK,
    SURFACE_PAUSE,
    OWNERSHIP_RETAINED
}
