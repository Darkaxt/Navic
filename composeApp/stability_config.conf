// Compose compiler stability configuration.
// Mark Map parameter types as stable so composables that take a Map (e.g. CoverArt's and
// ArtGridItem's imageRequestHeaders: Map<String, String>) are skippable.
kotlin.collections.Map
kotlin.collections.MutableMap
java.util.Map
